---
// ADC-IMPLEMENTS: <home-feature-homepage-01>
//
// The homepage at `/`. Hero-only per <home-feature-homepage-01>: the engine
// slot IS the homepage. Site identity overlays the engine canvas; navigation
// lives in the shell header (provided by ShellLayout).
//
// Replaced wholesale from the Phase 1 placeholder. Phase 2 lands the real
// hero + engine slot per contracts/ADC_003_HOMEPAGE_AND_ENGINE_SLOT.md.
import ShellLayout from "../layouts/ShellLayout.astro";
import Hero from "../components/Hero.astro";
---

<ShellLayout title="milodowling">
  <Hero />
</ShellLayout>
